{
  "name": "cosmic-dolphin-workspace",
  "version": "1.0.0",
  "description": "Cosmic Dolphin Monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/shared build && bun run --cwd packages/api build && bun run --cwd packages/worker build",
    "dev": "bun run  build && concurrently \"bun --env-file=../../.env.dev run --cwd packages/api dev\" \"bun --env-file=../../.env.dev run --cwd packages/worker start:dev\"",
    "start": "concurrently \"bun run --cwd packages/api start\" \"bun run --cwd packages/worker start:prod\"",
    "clean": "bun run --cwd packages/shared clean && bun run --cwd packages/api clean",
    "lint": "bun run --cwd packages/api lint && bun run --cwd packages/worker lint",
    "test": "bun run --cwd packages/shared test && bun run --cwd packages/api test && bun run --cwd packages/worker test",
    "test:db:up": "docker compose -f docker-compose.test.yml up -d",
    "test:db:down": "docker compose -f docker-compose.test.yml down -v",
    "local:db:up": "docker-compose -f docker-compose.test.yml up",
    "local:db:down": "docker-compose -f docker-compose.test.yml down -v",
    "local:db:reset": "bun run local:db:down && bun run local:db:up",
    "test:db:reset": "bun run test:db:down && bun run test:db:up",
    "docker:clean": "docker system prune -f",
    "docker:clean:all": "docker system prune -a -f --volumes",
    "docker:clean:build": "docker builder prune -f",
    "docker:clean:images": "docker image prune -a -f",
    "docker:clean:containers": "docker container prune -f",
    "install:all": "bun install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "shadcn": "^3.3.1"
  },
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "@ai-sdk/azure": "^2.0.30",
    "@ai-sdk/openai": "^2.0.28",
    "@openrouter/ai-sdk-provider": "^1.2.0",
    "ai": "^5.0.41",
    "ollama-ai-provider-v2": "^1.3.1",
    "zod-to-json-schema": "^3.24.6"
  },
  "trustedDependencies": [
    "@nestjs/core"
  ]
}