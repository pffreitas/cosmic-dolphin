import { DesignSystemPrototype } from "@/components/design-system-prototype/DesignSystemPrototype";

// PROTOTYPE: throwaway design exploration for Cosmic Dolphin's design system.
// Three visual systems are switchable via ?system=blue|amber|mono and the
// screen can be changed with ?screen=home|library|detail. Mock data only.
export default function DesignSystemPrototypePage() {
  return <DesignSystemPrototype />;
}
